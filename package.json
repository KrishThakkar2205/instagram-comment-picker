{
  "name": "instagram-comment-picker",
  "version": "2.0.0",
  "description": "Pick winners from Instagram Reel comments — deployed on Vercel",
  "scripts": {
    "start": "node server.local.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cookie": "^0.6.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
